# ArtCraft Client Dependencies
# No external dependencies - uses subprocess for CLI calls
# Optional dependencies for enhanced features:

# For async support
# asyncio-throttle>=1.0.0

# For testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# For type hints (development)
# mypy>=1.0.0
